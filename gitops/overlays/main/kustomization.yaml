apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: main

resources:
  - ../../base/podinfo

patches:
  - path: replica-patch.yaml

commonLabels:
  env: main
  managed-by: argocd
