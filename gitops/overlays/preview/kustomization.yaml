apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: preview-placeholder

resources:
  - ../../base/podinfo

patches:
  - path: replica-patch.yaml

commonLabels:
  env: preview
  managed-by: argocd
